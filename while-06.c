#include <stdio.h>

// s = 1 + 3 + 5 +  .... + n

int main (void)
{
    int a, b, i;

    printf("Enter the values of a and b: ");
    scanf("%d %d", &a, &b);

    i=a;
    while (1) {
        i = i - 1;
    }
    return 0;
}
